<div class="row">
	<h2 id="title">HandStack</h2>
</div>

<p class="lead button">  
  <button {{action "login"}} class="btn btn-default">
    {{fa-icon "facebook"}}
      Sign in with Facebook
  </button>
</p>

{{#if session.isAuthenticated}}
  <div class="collapse navbar-collapse">
    <ul class="nav navbar-nav">
      <li> {{#link-to "dashboard"}} All {{/link-to}} </li>
    </ul>

    <p class="navbar-text navbar-right avatar"><img src={{session.currentUser.facebook.cachedUserProfile.avatar_url}} class="img-circle" /></p>
  </div>
{{/if}}


{{outlet}}